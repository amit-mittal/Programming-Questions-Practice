#include<stdio.h>

int main()
{
	unsigned long long int test,num,n,i,count,p;

	scanf("%lld",&test);

	while(test--)
	{
		scanf("%lld",&num);
		count=0;

		printf("%lld\n",250*(num-1)+192);
	}

	return 0;
}
