#include<stdio.h>
#include<iostream>
#include<cstring>

using namespace std;

int main()
{
	scanf("%d %d",&a,&b);
	
	while(a!=0||b!=0)
	{
		b=b%a;
		
		for(i=0;i<a;++i)
			scanf("%d",&x[i]);
			
		getchar();
		gets(string);
		
		len=strlen(string);
		
		for(i=0;i<len;++i)
		{
			
		}
		
		scanf("%d %d",&a,&b);
	}

	return 0;
}
