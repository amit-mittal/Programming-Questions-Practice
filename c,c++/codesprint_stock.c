#include<stdio.h>

int main()
{
	int test,num,a[50005]={0},sum=0;

	scanf("%d",&test);
	
	while(test--)
	{
		scanf("%d",&num);
		for(i=0;i<num;++i)
			scanf("%d",&a[i]);
		
		for(i=0;i<num;++i)
		{
			if(a[i]>sum)
			{
				
			}
			else if(a[i]<sum)
			{
			}
		}
	}
	
	return 0;
}
